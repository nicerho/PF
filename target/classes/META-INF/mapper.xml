<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pfDB">
	<insert id="adminInsert">
		insert into admin values('0',#{abranch},#{adep},#{apos},#{aid},#{aname},#{apw},#{aemail},#{atel},'Y',now());
	</insert>
	<select id="login" resultType="ad" parameterType="string">
		select * from admin where aid=#{insertId}
	</select>
	<select id="findAdminByDep" resultType="ad" parameterType="string">
		select * from admin where adep=#{adep} order by ano desc;
	</select>
	<select id="selectAll" resultType="ad">
		select * from admin order by ano desc;
	</select>
	<select id="selectByName" resultType="ad" parameterType="string">
		select * from admin where aname=#{aname} order by ano desc;
	</select>
</mapper>